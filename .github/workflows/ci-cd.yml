name: Social Media Clone CI/CD

on:
  push:
    branches: ["main"]

jobs:

  build-and-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: socialdb
          POSTGRES_USER: social
          POSTGRES_PASSWORD: social
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: ✅ Checkout code
        uses: actions/checkout@v4

      # ---------------- BACKEND BUILD ----------------
      - name: ☕ Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"

      - name: 🔥 Build Backend + Run Tests
        working-directory: backend
        run: |
          mvn clean verify \
            -Dspring.datasource.url=jdbc:postgresql://localhost:5432/socialdb \
            -Dspring.datasource.username=social \
            -Dspring.datasource.password=social

      # ---------------- FRONTEND BUILD ----------------
      - name: ⚛️ Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: 🔥 Build Frontend
        working-directory: social-ui
        run: |
          npm install
          npm run build

  docker-build:
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.ref == 'refs/heads/main'

    steps:
      - name: ✅ Checkout code
        uses: actions/checkout@v4

      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # NOTE: Uncomment these lines after setting DOCKER_USER and DOCKER_PASS secrets in GitHub
      - name: 🔑 Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}

      # ---------------- BACKEND IMAGE ----------------
      - name: 🏗️ Build Backend Image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true # Change to true after configuring secrets
          tags: ${{ secrets.DOCKER_USER }}/social-backend:latest

      # ---------------- FRONTEND IMAGE ----------------
      - name: 🏗️ Build Frontend Image
        uses: docker/build-push-action@v5
        with:
          context: ./social-ui
          push: true # Change to true after configuring secrets
          tags: ${{ secrets.DOCKER_USER }}/social-ui:latest

  deploy:
    needs: docker-build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: ✅ Checkout code
        uses: actions/checkout@v4

      - name: 🚚 Copy Docker Compose to AWS EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          source: "docker-compose.prod.yml"
          target: "/home/${{ secrets.VPS_USER }}/social-app"

      - name: 🚀 Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          script: |
            cd /home/${{ secrets.VPS_USER }}/social-app
            export DOCKER_USER=${{ secrets.DOCKER_USER }}
            
            # Login to Docker Hub (optional but recommended for private)
            # echo "${{ secrets.DOCKER_PASS }}" | docker login -u "${{ secrets.DOCKER_USER }}" --password-stdin

            docker compose -f docker-compose.prod.yml pull
            docker compose -f docker-compose.prod.yml up -d
            docker image prune -f
